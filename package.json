{
  "name": "weather-now",
  "version": "1.0.0",
  "private": true,
  "description": "Weather Now - quick current weather lookup using Open-Meteo (non-commercial, demo)",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  }
}
